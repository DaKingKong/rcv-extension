# Setup

- `npm i`
- create `config.json` under `/src` with below fields:

```json
{
  "clientId": "",
  "rcServer": "",
  "redirectUri": "",
  "server": ""
}
```

# Build Client

- `npm run build-ext`
- output is in `dist` folder

# Run with local server

Ask Da